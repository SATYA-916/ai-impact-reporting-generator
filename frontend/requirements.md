## Packages
canvas-confetti | For delightful celebration effects when a positive impact report is generated
@types/canvas-confetti | Type definitions for canvas-confetti

## Notes
The application expects `/api/products` to return an array of available eco-products.
The application expects `/api/impact-report` to generate and return the AI analysis.
The frontend uses a clean, minimal eco-friendly design language with subtle animations.
